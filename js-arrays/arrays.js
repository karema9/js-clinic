// array construction with String Literal with Constructor
console.log("Welcome to array tutorial");

const Students = new Array(4);
students = ["Jack", "Dick", "Tom", "Harry"];
console.log();
console.log(typeof students);

// using array literal
const answers = [42, "paper", -3, "plastic", 20.5];
console.log("Length of arrays >>", answers.length);

// Using an enhanced for loop to access element in an array
for(let i=0; i<answers.length; i++)
    console.log(answers[i]);

// modifying array contents
// answers[0] = "Hagler";
// answers[answers.length] = "Liston";

// print out array contents
// console.log("New array items >>");
// for(let i = 0; i < answers.length ; i++)
    // console.log(answers[i]);



// Array.splice
/**
var getElements = function iterate(array){
    for (let i of array)
        console.log(i);

/**
 * Removes a specified number of elements from a specified location
 */


}


